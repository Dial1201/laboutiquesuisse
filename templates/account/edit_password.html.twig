{% extends 'base.html.twig' %}


{% block content %}
	<h1>Modier mot de passe</h1>
	<a href="{{ path('account') }}">Retourner à mon compte</a>
	<hr>
	{% if notif %}
		<div class="alert alert-info">
			{{notif}}
		</div>
	{% endif %}
	{{ form(form)}}
		<hr>
	{% endblock %}
